<Window x:Class="WpfApp1.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:viewmodel="clr-namespace:WpfApp1.ViewModel"
        mc:Ignorable="d"
        Title="Menu"  Height="1080" Width="1920">

        <Window.DataContext>
            <viewmodel:MenuViewModel/>
        </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="31*"/>
        </Grid.ColumnDefinitions>
        <Image Source="Images/italy2.png" Stretch="Fill" Margin="0,-2,0,-2" Grid.ColumnSpan="2"></Image>
        <Button Content="←" 
    HorizontalAlignment="Left" 
    Height="154" 
    VerticalAlignment="Top" 
    Width="206" FontSize="100" 
    Cursor="Hand" 
    FontFamily="Italia Std Book" 
    Foreground="green" Margin="10,900,0,0" Grid.ColumnSpan="2" Command="{Binding GoBackCommand}" >

            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="Transparent"
        CornerRadius="30"
        BorderBrush="Red"
        BorderThickness="0" Margin="0,0,14,50">
                        <ContentPresenter HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Label Content="Menu" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="160" FontFamily="Italia Std Book" Foreground="white" Width="410" Margin="1397,28,0,0" Grid.Column="1"/>

    </Grid>
</Window>
